class Punto():
    def __init__(self, lat, long, distancia, referencia):
        self.lat = float(lat)
        self.long = float(long)
        self.distancia = float(distancia)
        self.referencia = referencia
